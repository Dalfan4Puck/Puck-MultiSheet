# Asset bundle (optional — not used for vanilla multi-rink)

Only needed if `UseAssetBundle: true` in `config/multi_rink.json` (Jake-Porter open-world TestLevel).

**Default PHL setup uses the base-game rink cloned in code — no bundle required.**

Place Unity-built bundle here as `puckobjects` when using the legacy bundle path.
